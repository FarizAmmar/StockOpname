import Authenticated from "@/Layouts/AuthenticatedLayout";

// Default transaction view
const Transaction = () => {
    return (
        <Authenticated title="Transaction">
            <div>This is transaction pages</div>
        </Authenticated>
    );
};

export default Transaction;
